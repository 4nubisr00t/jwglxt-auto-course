# 任务：为 jwglxt-auto-course 制作桌面快捷方式图标（.ico）

你是本项目（湘潭大学教务选课自动化工具）的协作者。请以 `D:\hanako\jwglxt-auto-course` 为工作目录，先读一遍 `HANDOFF_PROJECT.md` 与 `assets/logo.svg`，再动手。

## 一、任务总述

为「湘大选课·漆黑结界」程序制作一枚**桌面快捷方式图标**（`.ico`），视觉风格与 GUI 界面完全同源：暗夜紫 × 樱花粉 × 星辉浅紫的二次元结界风。完成后：
1. 替换 `assets/icon.ico` —— **窗口标题栏图标、任务栏图标、桌面快捷方式图标共用这一份文件，不需要分做两套**（`gui_app.py` 第 37 行 `ASSET_ICON` 指向它，第 402 行 `iconbitmap` 用它设窗口图标，替换后两处自动生效）
2. 在桌面创建（或更新）指向本程序的快捷方式，使用新图标，双击无黑窗直接启动

**注意这是一枚「小图标」，不是海报。** 它会被压缩到 16px 显示在任务栏和桌面，设计逻辑与 `logo.svg`（512px 的大图）完全不同：主体必须极简、轮廓粗、对比强。沿用 logo.svg 的细节堆叠会糊成一团灰。

## 二、风格锚点（必须 100% 遵守）

### 配色（与 GUI/现有 logo 完全同源，不许新造颜色）
| 角色 | 色值 |
|---|---|
| 夜空底 | `#07040d` → `#12091c` → `#2a143d`（径向渐变，中心略亮） |
| 樱粉主色 | `#ff9ec4` / `#ff77a9` / `#ff2b5e` |
| 星辉紫 | `#e0d4ff` / `#b388ff` / `#6f42c1` |
| 描边/结界 | `#3a2360` / `#5d3988` |
| 高光 | `#ffffff`（少量，点睛用） |

### 元素语汇
- 魔法阵 / 结界环 / 八角星符文 / 星芒射线
- 樱花花瓣（飘落感）
- 魔晶菱核 / 契约之剑（象征极速精准狙击抢课）
- 「黑长直红眼」少女是 GUI 主题人格，可用其**红瞳意象**作为图腾（见设计方向）

### 禁忌（主人的审美红线，违反即返工）
- ❌ 不用 AI 生图那一套：不要 emoji 感、不要廉价的塑料渐变堆叠、不要「立体 3D 水晶球」质感
- ❌ 不要出现需要放大的文字/字母纹理（小尺寸必糊）
- ❌ 不要超过 4 类图形元素（小而干净，宁缺毋滥）
- ❌ 不准另起炉灶换配色，色卡就是法律

## 三、设计方向（三选一，做完自评后选最优）

1. **红瞳结界（推荐）**：深紫圆底中心一只猩红竖瞳（低饱和暖红 `#ff2b5e` 做瞳 + `#ff9ec4` 高光），瞳外一圈粗线条八角星魔法阵简化环，四角飘落 2~3 片樱花。16px 下看到的是「紫圆盘中一点红」，辨识度最高，也最贴合「黑长直红眼」人格。
2. **契约之剑（备选 A）**：沿用 logo.svg 的魔晶光剑，剑刃樱粉渐变、剑格星辉紫，外裹简化结界圆环。比方案 1 更有「狙击抢课」的速度感，但 16px 下剑形容易糊，需要加粗到 5px+。
3. **现有 logo 简化重绘（备选 B）**：把 logo.svg 删减到「双八角星 + 核心菱核 + 2 片花瓣」，其余全砍。

## 四、技术规格

- 输出格式：`.ico`，必须包含多尺寸 `[16, 24, 32, 48, 64, 128, 256]`；同时导出一张 `256×256` 的 PNG 预览（`assets/logo_preview.png`）
- 制作方式：**用 Python PIL 以 1024×1024 为设计画布矢量式绘制**，然后 LANCZOS 逐级降采样到各尺寸，再合并存 ICO。PIL 绘制优先用 `ImageDraw` 的多边形/圆弧/粗线，不要手写逐像素
- 图标形状：四角圆角方形或正圆均可，背景要么透明、要么用深紫夜空底（选透明底则 16px 时需自查轮廓可见性，死黑边缘不允许）
- 小尺寸可读性硬规则：
  - 主体图形的最小明暗对比 ≥ 中等（别让樱粉和深紫糊在一起）
  - 1024 画布上主体轮廓粗 ≥ 5px，元素总数 ≤ 4 类
  - **自检**：生成后写一个脚本把 256px 缩小到 16px 输出放大 8 倍的预览图，肉眼确认「还是一眼能认出那是个结界+红瞳/剑」

## 五、实施步骤

1. 读 `HANDOFF_PROJECT.md`、`assets/logo.svg`、`gui_app.py` 中 ASSET_ICON 的引用
2. 写设计脚本（建议 `make_icon.py` 放项目根目录，可复跑）：绘制 1024 画布 → 生成多尺寸 → 存 `assets/icon.ico` + `assets/logo_preview.png` → 输出 16px 放大自检图
3. 替换图标后，运行 `python gui_app.py` 确认窗口标题栏/任务栏图标正常（无报错即可，不必完成登录）
4. 桌面快捷方式：
   - 若桌面已有指向本程序的快捷方式（当前应没有），更新其图标
   - 否则用 WScript.Shell 创建：`TargetPath` 指向 `pythonw.exe`（找不到则退回 `python.exe`，注意探测路径），`Arguments="gui_app.py"`，`WorkingDirectory` 为项目目录，`IconLocation` 为 `assets/icon.ico`
   - 双击测试：窗口启动、无黑色 cmd 窗闪烁
5. 收尾时把 `make_icon.py` 顺手加进项目（不改动其他源码）

## 六、验收清单（全部通过才算完成）

- [ ] `assets/icon.ico` 含 16~256 全部尺寸，ICO 结构完整
- [ ] 16px 放大预览可辨识主体（结界环 + 红瞳 / 剑）
- [ ] 深色桌面与任务栏上图标轮廓清晰，无死黑边缘、无糊团
- [ ] `gui_app.py` 启动后窗口图标已切换为新版
- [ ] 桌面快捷方式双击即启动，无黑窗
- [ ] 配色全部来自色卡，无新造颜色
- [ ] 旧 `assets/icon.ico`、`logo.svg` 未删除（可另存备份目录）

## 七、红线

- 不 `push`，改动留在本地
- 不动 `grab.py / jw_cdp_client.py / schedule.py` 的行为
- 只新增 `make_icon.py`，不改 `gui_app.py` 的启动逻辑之外的任何代码若确需改，先停下来说明
- 不删除任何现有素材

---

*风格关键词：漆黑结界 · 暗夜紫×樱花粉 · 黑长直红眼 · 少 AI 味 · 小尺寸可读性优先*