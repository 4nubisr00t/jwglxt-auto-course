# JWGLXT AUTO · 项目交接文档（正式版）

> 湘潭大学教务选课自动化工具。Python 桌面程序：CDP 读浏览器登录态 → 直打教务接口 → 带冲突检测的抢课 + 二次元氛围前端。
> 交接主编：Mako · 2026-09-04

---

## 1. 仓库与位置

| 项 | 值 |
|---|---|
| 仓库名 | `jwglxt-auto-course` |
| 远程 | `https://github.com/4nubisr00t/jwglxt-auto-course`（owner: 4nubisr00t，合作者 fanwang555） |
| 本地 | `D:\hanako\jwglxt-auto-course` |
| 当前分支 | `main`，HEAD `87e3a1b`，已 push，本地与远程同步 |
| 累计提交 | 16 个（含 v0.1.0 上游 + Mako/Antigravity 迭代） |

## 2. 运行方式

```bash
cd D:\hanako\jwglxt-auto-course
python gui_app.py        # 依赖：customtkinter, requests, websocket-client, pillow
# 也可双击目录下的 run_gui.bat
```

## 3. 文件职责

| 文件 | 职责 | 可否改 |
|---|---|---|
| `gui_app.py` | 前端（二次元结界风，约 1200 行） | ✅ 主改对象 |
| `grab.py` | 抢课核心逻辑 + 全表抓取（GUI 数据源） | ❌ 只读调用 |
| `jw_cdp_client.py` | CDP 会话 / 教务接口封装 | ❌ 只读调用 |
| `schedule.py` | sksj 时间解析 + 冲突判定 | ❌ 只读调用 |
| `crawl_all.py` / `test_*.py` / `verify_cdp.py` | 工具/测试脚本 | 不用管 |
| `assets/` | 素材（bg_main.jpg 背景插画等） | 可替换 |

## 4. 已完成的全部功能（不要再动）

**后端链路**
- 门户 CAS 登录 → 自动拉起托管 Chrome → 自动打开教务选课页（拿 rwlx 等 hidden 参数）→ 连接成功
- 已选课读取 + 时间槽解析 → 作为「防冲突基准」
- 抓全表：四类课程（01/06/10/11）503+ 门，逐课 get_jxbs 补时间/地点/教师，5 线程并发（约 90s，带进度日志）
- 抢课循环：余量优先选班 + 剔除与已选课冲突的班（全冲突不硬抢）+ 提交状态分类 + 停止事件 + 预演模式
- 班次可视化 / 时段筛课（★你已选区 + 可选列表区，已选不重复进可抢列表）

**前端（二次元结界主题）**
- 背景插画 `assets/bg_main.jpg`：黑长直少女 + 脚下魔法阵 + 弯月樱花（Gemimage 生成，右对齐 cover 裁剪保人物）
- 日志为「真透文字层」：canvas create_text 直接浮于背景上，网点半透明底 + 描边文字，可滚轮回看
- 全局幼圆字体（标题华文琥珀），各控件字号已 +1~2
- 选项卡 canvas 手绘图标：樱花/四角星/书本
- 卡片发光缎带、按钮贴底等高、宽屏/对话布局切换、结界感知状态卡、影华口头禅彩蛋

## 5. 当前 UI 结构（接手前先读）

```
App (1060x740, minsize 900x620)
├── NightSky: tk.Canvas 全窗背景（bg_main.jpg 贴图 + 45 星 + 28 樱花动画）
│             └─ 无图时降级：程序化星空 + 手绘少女剪影
├── head 标题栏: ❖ JWGLXT(琥珀) AUTO(粉) 「湘大选课·漆黑结界」状态灯 + 底部发光线
├── left_frame (CTkScrollableFrame, 0.015/0.345/0.892):
│   ├─ 契约链路卡: 连接(粉) / 抓取+班次(并排) / 时段+自检(并排) / CDP 验证
│   ├─ 捕获目标卡: 关键词输入 + canvas 选项卡（手绘图标）
│   ├─ 运行参数卡: 轮询/超时 + 预演/组合实践开关
│   ├─ 结界感知卡: 3 行状态（回路/已选基准/全表缓存）
│   ├─ [弹性占位把按钮组推到底部]
│   └─ 开始(粉) / 停止(红描边)，等高 38
├── log_tool 工具条: 魂之回响标题 + 清空 + 宽屏/对话切换
└── 日志文字层(_draw_log): 画在 sky canvas 上，13pt 幼圆，行距 28，
            网点底 #0e0a1e stipple=gray50，描边字，滚轮历史
```

## 6. 遗留问题 / 待办（主人最新反馈，接手方优先处理）

1. **字体的「可爱感」还没完全让主人点头**：已全局切幼圆、标题琥珀，但主人仍觉得偏「死板」。可继续的方向：
   - 标题尝试华文行楷/华文彩云；正文试更大的幼圆（14~15pt）
   - 给卡片标题、状态灯加更俏皮的字重/颜色点缀
2. **右侧日志文字是重点**：主人原话「右边字小得几乎无法看见」——当前已升到 13pt，但仍需实际打开确认可读性；若还嫌小，日志字号可再升到 14~15，行距 28→32，并把描边加粗（画两层阴影）
3. **选项卡手绘图标**：已从字库符号换成 canvas 手绘（樱花/星/书），主人要「符合主题」——可继续精致化（花瓣边缘内凹、星芒更细、图标 hover 微动效）
4. 其余已解决项（黑框、留白、按钮比例、AI 味立绘→换生成图）均已落地，**不要退回归**

## 7. 关键代码接续点（改 UI 别碰坏这些）

- `self.client / snapshot / choosed_rows / busy_slots / stop_evt / _running / _wide_log / log_lines` 语义不变
- 日志全部走 `_append_log(text, tag)`（内部维护 log_lines + 重绘）
- 选项卡点击 `_on_cat_click` → 改 `self.cat[k]` → 重绘 `_draw_cat_tabs`
- 布局切换 `_apply_layout()` → `_update_log_area(rx, rw)` 换算像素区
- 背景贴图：`NightSky.init(w,h)` 用 `ASSET_BG`（`assets/bg_main.jpg`），缺失自动降级
- **customtkinter 的 place() 不接受 width/height 参数**（必须给构造函数的坑）
- 左栏是可滚动 frame，内容高度 < 可视高度时底部由弹性占位补足（已处理全屏留白）

## 8. 红线

- **不 push**：除非主人明确说「push」（目前主线已经推到远程，后续改动默认留在本地）
- 不动 `grab.py / jw_cdp_client.py / schedule.py` 的行为（前端只调用）
- 改完 `python gui_app.py` 自测：启动无报错、全屏不畸形、按钮完整可点、选项卡点击正常、日志可读

## 9. 提交历史（16 commits 概览）

- `f646d2d` → `208a44c`：后端（import 修复→门户入口→竞态消除→抓全表补时间→自动开选课页）
- `f8a129b` → `b169606`：Mako 前端（夜空动画→卡片美化→手绘选项卡）
- `66c1c57` / `49915ab`：Antigravity 大重构（黑长直结界 UI、防裁切、结界感知卡）
- `ac3088c` → `87e3a1b`：Mako 收尾（宽屏默认→撑满→按钮贴底→背景插画→幼圆字体/手绘图标）

---

*交接人：Mako · 主人指定的审美规则：暗夜紫×樱花粉、黑长直红眼、结界魔法阵、少 AI 味、可读性优先。*